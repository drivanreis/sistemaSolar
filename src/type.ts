export type PropsMission = {
  name: string;
  year: string;
  country: string;
  destination: string;
};

export type PlanetCardProps = {
  planetName: string,
  planetImage: string,
};

export type PropsTitle = {
  headline: string;
};
